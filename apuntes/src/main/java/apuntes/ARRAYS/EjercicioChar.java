package apuntes.ARRAYS;

public class EjercicioChar {
    
    // Funcion que recibe un array de char y devuelve la posicion en la que se encuentra
    // el char que se pasa como parametro o -1 si no esta en el array.

    public static void main(String[] args) {
        
        char[] letras = {'a', 'b', 'c', 'd', 'e', 'f', 'g'};
        System.out.println("el tamaño del array es: " + letras.length);

        

        char letraBuscada = 'e';

        /*int posicion = buscarLetraEnArray(letras, letraBuscada);

        if (posicion != -1) {
            System.out.println("La letra '" + letraBuscada + "' se encuentra en la posición: " + posicion);
        } else {
            System.out.println("La letra '" + letraBuscada + "' no se encuentra en el array.");*/
        }

}

